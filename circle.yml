machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t pgrimaud/postgresql:9.3 .
test:
  override:
    - docker run -d --name=postgresql1 pgrimaud/postgresql:9.3 ; sleep 10
